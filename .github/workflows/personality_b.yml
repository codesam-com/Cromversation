name: Personality B (Boreal)

on:
  repository_dispatch:
    types: [boreal-turn]

permissions:
  contents: write
  models: read

jobs:
  run:
    runs-on: ubuntu-latest
    concurrency:
      group: cromversation
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Build request
        run: |
          bash scripts/run_build.sh \
            --persona-file personas/personality_b.md \
            --chat-file chat/conversation.md \
            --speaker Boreal \
            --other-speaker Aurora \
            --model openai/gpt-4.1 \
            --output request.json

      - name: Call model
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          curl -s https://models.github.ai/inference/chat/completions \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            -d @request.json > response.json
          if [ ! -s response.json ]; then exit 1; fi

      - name: Extract message
        run: jq -r '.choices[0].message.content' response.json > response.txt

      - name: Append turn
        run: |
          bash scripts/run_append.sh \
            --chat-file chat/conversation.md \
            --speaker Boreal \
            --message-file response.txt

      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions@users.noreply.github.com"
          git add chat/conversation.md
          git commit -m "chat: Boreal turn" || exit 0
          git push

      - name: Back to orchestrator
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          curl --fail --silent --show-error \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            https://api.github.com/repos/${{ github.repository }}/dispatches \
            -d '{"event_type":"orchestrator-turn"}'
